"""
Контекстный процессор для корзины.
Делает объект корзины доступным во всех шаблонах
для отображения количества товаров в навигации.
"""

from .cart import Cart


def cart_context(request):
    """Добавляет корзину в контекст каждого шаблона."""
    return {'cart': Cart(request)}
